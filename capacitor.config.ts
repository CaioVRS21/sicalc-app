import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meuapp.sicalc',
  appName: 'sicalc-app',
  webDir: 'dist/sicalc-app/browser'
};

export default config;
